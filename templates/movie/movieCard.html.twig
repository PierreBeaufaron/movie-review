<div class="break-inside-avoid-column rounded-lg border-y-gray-300 border shadow-lg mb-4">
    <img src="" alt="" class="h-40 rounded-t-lg bg-slate-300">
    <div class="p-4 ">
        <h2 class="text-2xl font-bold capitalize mb-5">{{ movie.title }}</h2>
        <p>{{ movie.genre.name }}
        <br><b class="font-bold">{{movie.director.name}}</b>
        <br>{{movie.releaseDate|date('Y')}}</p>
        <p><a href="/movie/{{movie.id}}" class="inline-block py-1 px-4 rounded-md bg-blue-500 text-white text-sm mt-2 transition duration-300 ease-in-out hover:bg-cyan-800">En savoir + >>></a></p>
    </div>
</div>